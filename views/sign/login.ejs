<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <!-- Bootstrap -->
    <link href="css/base.css" rel="stylesheet" media="screen">
    <link href="css/bootstrap.css" rel="stylesheet" media="screen">
    <style type="text/css">
      body {
          padding-top: 40px;
          padding-bottom: 40px;
          background-color: #f5f5f5;
      }

      .form-signin {
          max-width: 300px;
          padding: 19px 29px 29px;
          margin: 0 auto 20px;
          background-color: #fff;
          border: 1px solid #e5e5e5;
          -webkit-border-radius: 5px;
          -moz-border-radius: 5px;
          border-radius: 5px;
          -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
          -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
          box-shadow: 0 1px 2px rgba(0,0,0,.05);
      }
      .form-signin .form-signin-heading,
      .form-signin .checkbox {
          margin-bottom: 10px;
      }
      .form-signin input[type="text"],
      .form-signin input[type="password"] {
          font-size: 16px;
          height: auto;
          margin-bottom: 15px;
          padding: 7px 9px;
      }
      .back{
          width: 360px;
          margin: -10px auto 0;
          text-align: center;
      }
      .back-to{
          color: #333;
          font-size: 14px;
          padding: 8px 10px;
          background: #eee;
          display: block;
      }
      .alert{
          margin: auto;
          width: 310px;
      }
    </style>
</head>
<body>
    <% if( typeof error == 'object' ){ %>
        <% if(error.length == 1 ){ %>
        <div class="alert alert-error">
            <h4>hey，有错误了。</h4>(￣﹏￣) <%= error[0] %>
        </div>
        <% }else{ %>
        <div class="notice-error">
            <div class="msg-inner">
                (￣﹏￣) hey，有错误了。
                <ul>
                    <% for(var i = 0, len = error.length; i < len; i++){ %>
                    <li><%= error[i] %></li>
                    <% } %>
                </ul>
            </div>
        </div>
        <% } %>
    <% } %>
    <div class="container fn-mt10">
        <form id="J-logForm" class="form-signin" method="post">
            <h2 class="form-signin-heading">欢迎回家。</h2>
            <input id="J-username" type="text" class="input-block-level" name="username" placeholder="用户名">
            <input id="J-password" type="password" class="input-block-level" name="password" placeholder="密码">
            <label class="checkbox">
                <input type="checkbox" value="remember-me"> 记住我
            </label>
            <button class="btn btn-large btn-primary" type="submit">登 录</button>
            <input class="btn btn-large" type="button" value='注册一个账号' onclick="location.href='/reg'">
        </form>
        <div class="back">
            <a class="back-to" href="/"><i class="icon-home"></i> 首页</a>
        </div>
    </div>
</body>
  <script type="text/javascript">
      var get = function(id){ return document.getElementById(id) };
      var form = get('J-logForm'),
              username = get('J-username'),
              pass = get('J-password');

      form.onsubmit = function(){
          if(username.value == ''){
              alert('用户名拿来！');
              username.focus();
              return false;
          };
          if(pass.value == ''){
              alert('密码在哪？');
              pass.focus();
              return false;
          }
      };
  </script>
</html>