<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <!-- Bootstrap -->
    <link href="css/base.css" rel="stylesheet" media="screen">
    <link href="css/bootstrap.css" rel="stylesheet" media="screen">
    <style type="text/css">
      body {
          padding-top: 40px;
          padding-bottom: 40px;
          background-color: #f5f5f5;
      }

      .form-signin {
          max-width: 300px;
          padding: 19px 29px 29px;
          margin: 0 auto 20px;
          background-color: #fff;
          border: 1px solid #e5e5e5;
          -webkit-border-radius: 5px;
          -moz-border-radius: 5px;
          border-radius: 5px;
          -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
          -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
          box-shadow: 0 1px 2px rgba(0,0,0,.05);
      }
      .form-signin .form-signin-heading,
      .form-signin .checkbox {
          margin-bottom: 10px;
      }
      .form-signin input[type="text"],
      .form-signin input[type="password"] {
          font-size: 16px;
          height: auto;
          margin-bottom: 15px;
          padding: 7px 9px;
      }
    </style>
</head>
<body>

    <% if( typeof error == 'object' ){ %>
    <div class="notice-error">
        <div class="msg-inner">
            (￣﹏￣) hey，有错误了。
            <ul>
                <% for(var i = 0, len = error.length; i < len; i++){ %>
                <li><%= error[i] %></li>
                <% } %>
            </ul>
        </div>
    </div>
    <% }else if( error === 0 ){ %>
    <div class="notice-normal">
        <div class="msg-inner">
            <(￣︶￣)> 【<%= locals.current_user %>】，注册成功，欢迎加入我们。
        </div>
    </div>
    <% } %>
    <div class="container fn-mt10">
        <form class="form-signin" method="post">
            <h2 class="form-signin-heading">此刻，加入我们。</h2>
            <input type="text" class="input-block-level" name="email" placeholder="用户名">
            <input type="password" class="input-block-level" name="password" placeholder="密码">
            <input type="password" class="input-block-level" name="confirmPassword" placeholder="确认密码">
            <button class="btn btn-large btn-primary" type="submit">立即注册</button>
            <input class="btn btn-large" type="button" value='已有账号，登录' onclick="location.href='/login'">
        </form>
    </div>
</body>
</html>